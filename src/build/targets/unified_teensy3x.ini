[env:teensy_30]
extends = env_common_teensy
board = teensy30
build_unflags =
    ${common.build_unflags}
build_flags =
    ${common.build_flags}
build_type = release

[env:teensy_31]
extends = env_common_teensy
board = teensy31
build_unflags =
    ${common.build_unflags}
build_flags =
    ${common.build_flags}
build_type = release

[env:teensy_32]
extends = env_common_teensy
board = teensy31
build_unflags =
    ${common.build_unflags}
build_flags =
    ${common.build_flags}
build_type = release

[env:teensy_35]
extends = env_common_teensy
board = teensy35
build_unflags =
    ${common.build_unflags}
build_flags =
    ${common.build_flags}
build_type = release

[env:teensy_36]
extends = env_common_teensy
board = teensy36
build_unflags =
    ${common.build_unflags}
build_flags =
    ${common.build_flags}
build_type = release
