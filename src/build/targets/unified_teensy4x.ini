[env:teensy_40]
extends = env_common_teensy
board = teensy40
build_unflags =
    ${common.build_unflags}
build_flags =
    ${common.build_flags}
build_type = release

[env:teensy_41]
extends = env_common_teensy
board = teensy41
build_unflags =
    ${common.build_unflags}
build_flags =
    ${common.build_flags}
build_type = release
